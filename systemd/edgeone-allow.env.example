# Copy to /etc/default/edgeone-allow and adjust paths as needed

# EdgeOne IP list URL
# EDGEONE_IPS_URL=https://api.edgeone.ai/ips

# Output path included by nginx
# OUT=/etc/nginx/conf.d/edgeone_allow.conf

# Nginx verification and reload commands
# NGINX_TEST_CMD="nginx -t"
# RELOAD_CMD="systemctl reload nginx"

# Extra curl options (timeout, proxy, etc.)
# CURL_OPTS="-fsS --max-time 10"

# Whether to reload nginx on change (1=yes, 0=no)
# RELOAD=1
